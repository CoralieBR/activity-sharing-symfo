{% extends 'base-admin.html.twig' %}

{% block title %}ADMIN{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Hello Admin!</h1>
    <div class="formulaire1">
        <a href="{{ path('activite_index') }}" class="small-btn">Activités</a>
        <a href="{{ path('article_accueil_index') }}" class="small-btn">Articles (accueil)</a>
        <a href="{{ path('activite_categorie_index') }}" class="small-btn">Catégories</a>
        <a href="{{ path('groupe_index') }}" class="small-btn">Groupes</a>
        <a href="{{ path('membre_index') }}" class="small-btn">Membres</a>
        <a href="{{ path('message_contact_index') }}" class="small-btn">Messages</a>
    </div>

</div>
{% endblock %}
