<form method="post" action="{{ path('article_accueil_delete', {'id': article_accueil.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet article ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_accueil.id) }}">
    <button class="btn">Supprimer</button>
</form>
