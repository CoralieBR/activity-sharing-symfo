<form method="post" action="{{ path('article_accueil_delete', {'id': article_accueil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_accueil.id) }}">
    <button class="btn">Delete</button>
</form>
