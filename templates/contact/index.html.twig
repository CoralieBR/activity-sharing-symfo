{% extends 'base.html.twig' %}


{% block body %}

<div class="formulaire1">
    {{ form_start(form) }}
    <h1 class="tittle">Message Contact</h1>
    <br>
    {{ form_widget(form) }}
    <br>
    <button href="{{ path('accueil') }}" class="big-btn1">{{ button_label|default('Envoyer') }}</button>
    {{ form_end(form) }}
    <br>
</div>

{% endblock %}