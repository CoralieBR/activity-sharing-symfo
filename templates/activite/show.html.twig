{% extends 'base-admin.html.twig' %}

{% block title %}Activite{% endblock %}

{% block body %}
<a href="{{ path('activite_index') }}" class="small-btn">Retour</a>
<div class="formulaire1">
    <h1>Activite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite.id }}</td>
            </tr>
            <tr>
                <th>NomActivite</th>
                <td>{{ activite.nomActivite }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ activite.description }}</td>
            </tr>
            <tr>
                <th>Valide</th>
                <td>{{ activite.valide ? 'Oui' : 'Non' }}</td>
            </tr>
        </tbody>
    </table>
<div class="d-flex flex-wrap justify-content-start align-items-center">
    
    
    
<div>
    <a href="{{ path('activite_edit', {'id': activite.id}) }}" class="small-btn">Modifier</a>
</div>

    {{ include('activite/_delete_form.html.twig') }}
    </div>

    </div>
{% endblock %}
