<form method="post" action="{{ path('moment_delete', {'id': moment.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moment.id) }}">
    <button class="btn-supp">Supprimer</button>
</form>
