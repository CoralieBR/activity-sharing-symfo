<form method="post" action="{{ path('activite_categorie_delete', {'id': activite_categorie.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette catégorie d'activités ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite_categorie.id) }}">
    <button class="small-btn">Supprimer</button>
</form>
